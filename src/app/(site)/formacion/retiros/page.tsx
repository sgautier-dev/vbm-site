import type { Metadata } from "next";

import PageShell from "@/components/PageShell";

export const metadata: Metadata = { title: "Retiros" };

export default function RetreatsPage() {
  return <PageShell title="Retiros" />;
}
