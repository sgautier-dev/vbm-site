import type { Metadata } from "next";

import PageShell from "@/components/PageShell";

export const metadata: Metadata = { title: "Colabora" };

export default function CollaboratePage() {
  return <PageShell title="Colabora" />;
}
